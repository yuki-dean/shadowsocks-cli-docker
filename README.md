# shadowsocks-cli-docker
a docker for shadowsocks client, integrate the privoxy, support socks5, http, https

Requiment:
docker
make

To make docker image, run the command:
> make

A docker image yuwei/ss-cli will be built
